# Copy this file to .env for local/production configuration.
# Never commit .env to GitHub.

DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=epsilon_db

# Local: /PROJECT12
# Production at domain root: leave empty
BASE_URL=/PROJECT12
